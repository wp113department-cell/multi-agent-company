# Copy to .env.local and fill in. Never commit .env* — enforced by .gitignore.

# FastAPI backend URL. Next.js proxies /api/* to this (see next.config.mjs).
# Local dev: leave empty (defaults to http://localhost:8000).
# Production: set to your deployed backend's public URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
