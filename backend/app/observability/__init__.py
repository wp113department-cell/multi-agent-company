"""Observability — structured, trace-correlated logging.

logging_context.py — contextvar-based trace_id/task_id/agent_run_id
  propagation into every log record, JSON structured formatter. See that
  module's own docstring (audit_v1.md 4.7 #2 / Phase M) for the design.
"""
