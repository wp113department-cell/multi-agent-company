{
  "name": "@gridiron/shared-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "migrate:up": "dotenv -e ../../.env -- node-pg-migrate up -j ts --tsconfig tsconfig.migrations.json --dir migrations --migration-file-language ts",
    "migrate:down": "dotenv -e ../../.env -- node-pg-migrate down -j ts --tsconfig tsconfig.migrations.json --dir migrations --migration-file-language ts",
    "migrate:create": "node-pg-migrate create -j ts --migration-file-language ts --dir migrations",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "node-pg-migrate": "^7.9.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "dotenv": "^16.4.0",
    "dotenv-cli": "^7.4.0"
  }
}
